# Lane 4 process log — 2026-08-30 (Alan's PC)
*Supersedes/extends `ops/lane4/PROCESS_LOG_2026-08-30.md` (written before SOP v1.0 named this path).*

## Batch 1 — GHL ground truth + Todd import + first publish
1. **GHL full pull (read-only, #46):** 259 contacts + per-contact detail pass (attributionSource lives ONLY on the detail endpoint), 144 appointments w/ statuses, 200 opportunities, 256 conversations / 8,218 messages, payments (EMPTY — see register #65). Raw JSON per RAW-ORIGINALS LAW in `ops/archive/ghl/2026-08-30/raw/`, derived CSVs + `GHL_PULL_SUMMARY.md` + `ROSTER_RECONCILIATION.md` alongside. 16/79 roster unknowns resolved (10 cancelled, 6 noshow); 63 unresolvable — GHL left 109/144 appts at 'confirmed' forever (register #64).
2. **Todd import (the one approved write):** existing contact **`nIy2smghNYT9II3enmQv`** updated (he was already in GHL since 6/30 WITH phone +1 415-424-5720 — the package's "only true gap" was never a gap). Opportunity `trnXkuLcYMuTg4iDg8za` → SCIO "Closed"/won/$0, renamed for the 4% household deal; 12 notes oldest-first; 7 tasks due 9/5. API log: `ops/lane4/todd_import_log.json`. Notion writeback = Lane 1 (per LANE-SYNC).
3. **Dashboard published (register #1):** https://claude.ai/code/artifact/c6ad801c-50fc-49d3-847a-e6a8b0ddd392 — favicon ⚡, capabilities `{"mcp":{"servers":[{"server":"Notion","tools":["notion-query-data-sources"]}]}}`.
4. Branch `claude/lane4-ghl-pull` pushed; Lane 1 merged it to the vault branch same evening, so the planned draft PR is superseded (no gh CLI on this PC; browser + credential routes classifier-blocked — `winget install GitHub.cli` would restore the PR path for future lanes).

## Batch 2 — register directives picked up after pull
5. **Republished the dashboard to the SAME artifact URL** with the Every Person roster (LANE-SYNC 8/30 directive, register #63/#83 line). Version label `every-person-roster`.
6. **LANE-SYNC (from Lane 4): Notion live-layer is quota-capped.** `notion-query-data-sources` SQL mode returned "workspace has reached the usage limit for Query Data Source" twice today (publish time + 30 min later). The dashboard's live refresh uses this exact call, so on the current Notion plan viewers will intermittently see the audited snapshot instead of live data — that is the page's designed fallback, not a bug. Permanent fix = Notion Business (unlimited SQL). Candidate register item for Lane 1.
7. Read `CLAUDE.md`, `ops/SOP.md` laws, `ops/REGISTER.md`, `ops/prompts/ALL_PROMPTS_FULL.md` per the sync ritual.

## Still machine-bound, still human-only (BRIEF §B — nobody in any lane can do these)
Loom lockdown (3 public videos w/ SSNs) · Meta URL params on the two active campaigns · Teramind API token or daily CSV export (#49) · invite Jacob to Ads Manager (blocks Sep 1 A/B) · Notion pending join request.
