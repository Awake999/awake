# 2026-09-07 — Lane 1 process log

## Register #182 — client testimonials doc
- **Ask:** positive client quotes, verbatim, dated, named, sourced (audio vs text), with jump links, stored in GitHub.
- **Sources:** Fathom (296 recordings listed Jun 21→Sep 6; 18 client calls read in full), GHL 2026-08-30 export (256 conversations; inbound SMS/email sweep + per-person hand read).
- **Method:** one data table in `ops/tools/testimonials_build.py` renders `ops/data/CLIENT_TESTIMONIALS.md` + `.json`. Every row must carry a link (build fails otherwise). Verified 74/74 quotes byte-for-byte against the transcript line at that timestamp or the SMS body on that date.
- **Gotchas found:** (1) Fathom `recording_id` ≠ the number in the `fathom.video/calls/<n>` URL — three fetches failed until I used the `id:` field from `list_meetings`. (2) GHL email `direction` is unreliable on reply chains: two Alan-authored emails were stored as inbound and would have been quoted as client praise. (3) Matthew's "saint" = **Credit Saint**, a competitor's name. (4) Ed Choi 6/29 recording has no transcript.
- **Not swept:** GHL call recordings (needs authenticated pull from the PC launcher), Slack, Gmail.
- **Output:** 74 quotes / 9 people; strongest lines in the doc TLDR.

## Register #184 — SOP → skill
- Built `.claude/skills/client-testimonials/SKILL.md` (procedure) + `ops/sops/CLIENT_TESTIMONIALS_SOP.md` (law) + `ops/tools/testimonials_verify.py` (repo-side byte check).
- Archived 13 client Fathom transcripts to `ops/archive/calls/fathom/` so every testimonial row verifies in-repo (74/74). LANE-SYNC to Lane 3: folders follow the archive anatomy; INDEX.md not touched.
- Recordings watch: still 0 files under `ops/archive/ghl/recordings/` on every remote branch.

## Register #185 — team tracking Sep 6
- Sources read: EOD channel (Lynn SOD only), #alan-nguyen-leads, #alan-nguyen-booked-calls, #----urgent, #call-outcomes/#ig-leads/#dispute-updates/#sales-team-chat (all empty), Gmail (Sep-5 Teramind digest, cancellation, 3 appointment emails, 3 ManyChat), Fathom (2 recordings, summaries).
- Note for future days: Teramind column retires after this report.

## Register #186 — testimonial cards
- Rendering: HTML → headless Chromium (pre-installed) → PNG, cropped with Pillow (pip-installed this session). 1080² + 1080×1920 per quote; 46 PNGs, ~11 MB total.
- Guard: card text must be a contiguous substring of a verified quote in CLIENT_TESTIMONIALS.json; the generator exits otherwise.
- Backgrounds come from the person's own words with a timestamped link.
- Photos: GHL export has 0 profile photos; did not scrape the web for faces; photo slot + consent rule documented in LIBRARY.md.

## Register #187 — public (first-name) card variants
- Consent basis: client agreement at onboarding (Alan's statement, verbatim in the register). Prospects (Allen Sims, Gunjan Patel) excluded from the public set.
- Public variant = first name, single-initial monogram, same verbatim text/date/background/proof.
