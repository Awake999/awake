# Alan's INTERACTION PROTOCOL spec — 2026-08-30 evening (verbatim, raw original)
*Governs how every lane responds to every user prompt. Stored per VERBATIM LAW; checklist form at ops/data/INTERACTION_PROTOCOL_CHECKLIST.md; SOP integration pending Alan's confirm.*

Program into the SOP, built into all four lanes and into the project itself. All four lanes and new lanes will be using this as a base. Every time the user says what the user says, it is all broken down verbatim in checklist form. Everything is fully heard and understood, but it's just optimized in checklist form so it's easily checkable for big blocks of text. No original fidelity data is lost.

That checklist is then presented as a response to the user to confirm or deny if this is all correct and if there's anything else missing or anything else that needs to be added to the checklist. This checklist is then stored in the database, hard-coded, so it's always referenced in the future for the SOP.

From that, make sure that you add any suggestions or insights on how you want to add to the plan or contribute to the plan that would make it better, more optimized, and more improved, not only for AI but also for humans. It simultaneously needs to be user interface-friendly for beginners as well as masters, but not at the sacrifice of the beginners, in tandem, in harmony, and unity and synchronicity with how AI and computer code and all of that itself operates on that other end of the spectrum.

In the SOP, every lane and Claude agent in this project should be pointing towards responding with the checklist and everything that was just mentioned before. Afterwards, ask to execute, and then we'll execute that. At the end, after the execution is done, we're going to cross off in a table what was completed and what was not. We're going to say the status of each one and the percentage of completion of each one. These are all different columns, by the way. The next columns should be:

* The quality in which it was executed, as one column.
* Why it was rated that way.
* Any types of improvements that are suggested to be put into practice.
* A user-interactable link where they can just click the hyperlink and it jumps straight to the work or an example of the project.
* The before and after.
* A final column saying, "Hey, for this column, it's before it looked like this. Afterwards, it looks like this."

This is a prime example of what our process should be like for interacting with one another. Please respond to this in that exact way, and then confirm the SOP that's being integrated, the location for such, and any suggestions on how to navigate forward to make sure it's fully implemented. Make sure all future lanes and current lanes are connected to this, and we will be drawing from this as a standard operating procedure.

As always, in that standard operating procedure, make sure that, in the end, when the output is finally output, the response is then generated in terms of buttons for the user to make it easy to respond to with Claude suggestions as well, as well as a timeline tracker and a progress tracker of the micro and the macro.

Again, make sure, since there are so many things that are mentioned here, we want a systemized checklist of all this stuff. We want it stored so we don't forget it later. Please again make a link to where this stuff is stored and organized so we can always reference back and jump to it, just in case things are lost because of the nature of compaction.

As always, on top of everything else that I've said, in harmony and synchronicity with it, make any suggestions from your perspective and your understanding that would be best to optimize navigating accuracy and precision based off of user prompts, our execution, follow-through, and the quality of output relative to the bottlenecks that happen with AI interaction with the compaction, and a summary.

Also, as a final note, in the beginning of the response output, after you respond with the checklist before in the plan and your additions before we actually execute, clearly label at the very end what the suggested model to use is, as well as the effort level and the reasoning on why. Especially if we have to use ultra code also mention in that part of the SOP, which we're all adding, the estimated use of tokens, the estimated length of time to completion, and how long things could be sustainably output at that level with that effort before we start running into token limits. Take into account:

* the token limits
* the daily usage
* the monthly usage
* the weekly usage
* the Fable 5 usage
* the plan usage

Right now, we're on 20x, which is the highest max plan that we can do. You can also make suggestions for enterprise and stuff like that, or add on if we need to go over things of that nature, just to make sure it all makes sense. Give us the raw information first before you start to try to change the original decision based off of conserving token data. Give the raw data first, then the optimized plan second, and then third, options and how we navigate. Again, this all should be organized into the standard operating procedure so we don't have to keep repeating this over and over again.

Recently, we've been using Fable 5 medium for execution, but now, because this is so dense, I'm going to put it on Fable 5 high. If I need to put it on Fable 5, either extra, max, or ultra code, let me know for the plan and the execution. If we need to rerun the plan, knowing that it was high and that wasn't high enough and we need to do extra, max, or ultra code, please let me know. Always let us know in the execution process as well, because the last thing we want to do is execute on a low model and low effort that didn't actually get us to our goal and then not know that was the bottleneck.

Always, in your response, you should be identifying what model we used to push that prompt through. If you don't know, you should ask us, and then you should make the suggestion for the prompt for that model and effort numbers or effort as well. Of course, you can ask us to rerun it if needed, because that's important. We want everything to be quality, quality, and functionality first.
