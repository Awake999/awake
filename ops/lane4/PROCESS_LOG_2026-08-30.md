# Lane 4 process log — 2026-08-30 (Alan's PC)

## What ran
1. **TASK 1 — GHL full pull (read-only).** `ghl_full_pull.py` + `ghl_pull_details.py` against location `WFkoNzKa9J9PxhngsLfl`, Bearer = local PIT (read from `apw-intel/.env`, never committed). Raw JSON in `ops/archive/ghl/2026-08-30/raw/`, derived CSVs + `GHL_PULL_SUMMARY.md` alongside. UTMs come from each contact's `attributionSource` (detail endpoint only — the list endpoint omits it); campaign_id/adset_id/ad_id parsed from the attribution URL.
2. **TASK 2 — Todd LoGuidice import (the ONE approved GHL write, authorized 8/12).** Todd ALREADY EXISTED in GHL: contact `nIy2smghNYT9II3enmQv` (created 6/30, tags `name via lookup`, `call booked scio`) **with phone +1 415-424-5720 on file** — the import package's "only true gap" was never a gap in GHL. He also already had an open $0 SCIO-pipeline opportunity `trnXkuLcYMuTg4iDg8za` (created 8/26). Executed as an UPDATE: contact fields + 8 tags merged (§1–2), opportunity moved to SCIO Pipeline → "Closed", status **won**, $0 monetary value, renamed "$0 upfront / 4% success fee" (§3 — no stage named "Closed Won" exists; "Closed"+won is the location's equivalent), 12 notes posted oldest-first (§4 role, §5 timeline, §6 document links — GHL's API has no attach-file-to-contact call, so §6 landed as a links note), 7 tasks created due 9/5 (§7; task 2 amended to "verify the phone GHL has" rather than "no phone anywhere"). Full API log: `ops/lane4/todd_import_log.json`. **GHL Contact ID: `nIy2smghNYT9II3enmQv`** → paste into Todd's Notion record (GHL Contact ID + GHL Link fields).
3. **TASK 3 — dashboard published.** `ops/dashboard/apw_command_board.html` → **https://claude.ai/code/artifact/c6ad801c-50fc-49d3-847a-e6a8b0ddd392** (favicon ⚡, mcp capability: Notion / notion-query-data-sources). Caveat: the workspace's Notion SQL quota was exhausted during publish, so the live query's success shape could not be re-observed this session; the page's own error handling drops it to the audited snapshot (`tool_error` branch), which is what a viewer sees until quota resets.

## Findings that change the books
- **GHL Payments is EMPTY** — 0 orders, 0 transactions, 0 subscriptions, 0 invoices for the entire location. Nick S.'s $500 and Jill's installments are NOT evidenced in GHL; money moves entirely outside it (verify at the processor/bank). Michael M.: no payment found — as expected, nothing to flag.
- See `ops/archive/ghl/2026-08-30/GHL_PULL_SUMMARY.md` and `ROSTER_RECONCILIATION.md` for counts and how many of the roster audit's 79 unknown outcomes GHL statuses resolve.
